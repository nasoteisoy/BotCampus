# Phone roam-combat — STYLE LOCKS (Render A → Dev A)

Jesus: playables must match stilts. Each option has its **own** world — do not share palettes.

| Opt | Lock | Folder |
|-----|------|--------|
| A Arena Clear | Neon cyan/magenta top-down | `a/art/STYLE_LOCK.md` + `palette.json` |
| B Corridor Slash | Whimsy candy side | `b/art/STYLE_LOCK.md` + `palette.json` |
| C Shadow Takedown | Painterly iso noir | `c/art/STYLE_LOCK.md` + `palette.json` |

## Dev A checklist
1. Apply `palette.json` hexes to `game.js` fillStyles + `index.html` CSS (body/canvas/sticks/buttons).
2. Shape language: A geometric neon · B stubby candy · C lean stealth — keep very different.
3. When `art/player.png` (and enemy/guard sprites) exist, prefer `drawImage` over colored circles.
4. Sole-push Pages when wired. Render A does not push.

Stills remain the mood authority; locks are the runtime contract.
